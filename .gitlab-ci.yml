before_script: []
stages: [
  "build",
]

documentation:
  image: "timnn/texlive"
  stage: "build"
  script: [
      "cd documentation",
      "pdflatex --shell-escape Seminar.tex"
      ]
  artifacts:
    paths:
      - documentation/Seminar.pdf
